<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">
        <shape>
            <corners
                android:radius="40dp" />
            <solid
                android:color="@color/groupAccountDetailedProgressBarBg"
                android:angle="270"
                />
        </shape>
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners
                    android:radius="40dp" />
                <gradient
                    android:angle="0"
                    android:startColor="@color/groupAccountDetailedProgressBarStart"
                    android:endColor="@color/groupAccountDetailedProgressBarEnd"
                    android:type="linear" />
            </shape>
        </clip>
    </item>
</layer-list>